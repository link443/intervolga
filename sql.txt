3. БД 
Чем отличаются эти запросы:
1. SELECT * FROM a, b WHERE a.id=b.a_id;// выборка всех полей из таблиц `a` и `b`, где поле `id` таблицы `a` равно значанию поля `a_id` таблицы `b`.
2. SELECT * FROM a JOIN b ON a.id=b.a_id;// соединение таблиц `a` и `b` путем сведения их в одну через связь  поля `id` таблицы `a` и поля `a_id` таблицы `b`.
Пришлите ответ текстом


